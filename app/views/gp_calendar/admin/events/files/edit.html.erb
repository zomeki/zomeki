<%= render 'menu' %>

<%= form_for(:item, :url => gp_calendar_event_file_path(@content, event_id: @event_id, id: @item.id), :html => {:method => :put, :multipart => true}) do |f| %>
  <%= error_messages_for :item %>
  <%= render 'form', f: f, submit_label: '更新' %>
<% end %>
