<%-
return '' if item.tasks.empty?

published_at = item.find_task_by_name('publish')
closed_at = item.find_task_by_name('close')
-%>

<%- if published_at || closed_at -%>
<p class="form">公開日時設定</p>

<table class="show">
  <tr>
    <th>公開開始日時</th>
    <td><%=l published_at if published_at %></td>
  </tr>
  <tr>
    <th>公開終了日時</th>
    <td><%=l closed_at if closed_at %></td>
  </tr>
</table>
<%- end -%>
