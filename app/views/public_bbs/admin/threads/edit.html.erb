<%= form_for(:item, :url => public_bbs_thread_path, :html => { :method => 'put' }) do |f| %>
  <%= error_messages_for :item %>
  <%= render :partial => 'form', :locals => {:f => f} %>
  <%= submit :update %>
<% end %>
