<%= form_for(:item, :url => public_bbs_thread_responses_path(@content, @thread)) do |f| %>
  <%= error_messages_for :item %>
  <%= render :partial => 'form', :locals => {:f => f} %>
  <%= submit :create %>
<% end %>
