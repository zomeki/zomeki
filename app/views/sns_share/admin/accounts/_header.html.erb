<div class="mainMenu">
  <%= render 'layouts/admin/cms/menu', :current => :content %>
</div>

<div class="contentMenu">
  <%= link_to "#{SnsShare::Account.model_name.human}一覧", sns_share_accounts_path(content: @content), class: 'fixed' %>
</div>
