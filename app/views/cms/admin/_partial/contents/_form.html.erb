<% if defined?(container) && container != :tr %>
<table class="show">
<% end %>
  <%= concept_form f %>
  <tr>
    <th><%= @item.locale :model %></th>
    <td>
      <% if @item.id %><%= @item.model_name(:short) %><% else %>
      <%= f.select :model, Cms::Lib::Modules.contents, :include_blank => :true %><% end %>
    </td>
  </tr><tr>
    <th><%= @item.locale :name %></th>
    <td><%= f.text_field :name, :class => 'name' %></td>
  </tr><tr>
    <th><%= f.label :note %></th>
    <td><%= f.text_field :note %></td>
  </tr>
<% if defined?(container) && container != :tr %>
</table>
<% end %>
