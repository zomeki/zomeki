<%= render 'menu' %>

<%= form_for(:item, :url => gp_article_doc_file_path(@content, doc_id: @doc_id, id: @item.id), :html => {:method => :put, :multipart => true}) do |f| %>
  <%= error_messages_for :item %>
  <%= render 'form', f: f, submit_label: '更新' %>
<% end %>
