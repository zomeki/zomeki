<%= form_for(@item, :as => :item, :url => gp_article_docs_path) do |f| %>
  <%= error_messages_for :item %>
  <%= flash[:link_check_result] %>
  <%= render 'form', :f => f %>
  <%= submit (@content.approval_related? ? @item.state_options : @item.state_options.reject{|o| o.last == 'recognize' }) %>
<% end %>
