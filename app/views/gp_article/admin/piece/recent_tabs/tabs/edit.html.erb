<%= form_for(:item, :url => gp_article_piece_recent_tab_tab_path, :html => { method: 'put' }) do |f| %>
  <%= error_messages_for :item %>
  <%= render 'form', :f => f %>
  <%= submit :update %>
<% end %>
