<%= form_for(@item, :as => :item, :url => gp_article_comment_path, :html => {method: 'put'}) do |f| %>
  <%= error_messages_for :item %>
  <%= render 'form', f: f %>
  <%= submit :update %>
<% end %>
