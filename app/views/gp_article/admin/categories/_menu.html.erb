<%= render 'gp_article/admin/shared/menu', index_path: gp_article_category_type_category_categories_path(content: @content,
                                                                                                         category_type_id: @category_type.id,
                                                                                                         category_id: @parent_category.try(:id).to_i),
                                                  sub: (case action_name.to_sym
                                                       when :index; "#{@category_type.title}一覧"
                                                       when :show;  @category_type.title
                                                       when :new;   "#{@category_type.title}登録"
                                                       when :edit;  "#{@category_type.title}編集"
                                                       end) %>
