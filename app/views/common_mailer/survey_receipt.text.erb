「<%= @form_answer.form.content.name %>：<%= @form_answer.form.title %>」に回答が届きました。

<% @form_answer.form.public_questions.where("form_type <> 'free'").each_with_index do |question, index| %>
■<%= question.title %>
<%= @form_answer.answers.where(question_id: question.id).first.try(:content) %>

<% end %>
<% if false %>
  次のURLをクリックして回答を確認してください。
  <%= @form_answer.form.content.site.full_uri.sub(/\/\z/, '') %><%= survey_form_form_answers_path(content: @form_answer.form.content, form_id: @form_answer.form.id) %>
<% end %>