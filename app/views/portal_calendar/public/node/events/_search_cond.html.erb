<%#
# To change this template, choose Tools | Templates
# and open the template in the editor.
%>

<%= form_tag @calendar.current_month_uri, :method => :post, :class => 'search' do %>
  <%= portal_calendar_checkbox_tag_form(@genres, 'genre', 'id', 'title', @genre_keys) %>
  <%= portal_calendar_checkbox_tag_form(@statuses, 'status', 'id', 'title', @status_keys) %>
  <%= submit_tag '抽出' %>
<% end %>
