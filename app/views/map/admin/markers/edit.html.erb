<%= form_for(:item, :url => map_marker_path, :html => { multipart: true, method: 'put' }) do |f| %>
  <%= error_messages_for :item %>
  <%= render 'form', :f => f %>
  <%= submit :update %>
<% end %>
