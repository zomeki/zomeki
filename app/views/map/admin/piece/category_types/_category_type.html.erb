<%-
common_attrs = {id: nil}
category_type_id = if category_type
                     category_type.id
                   else
                     common_attrs.merge!(disabled: true)
                     nil
                   end
-%>
<span style="<%= 'display: none;' if common_attrs[:disabled] %>">
  <%= select_tag 'category_types[]', options_for_select(category_types_for_option, category_type_id), common_attrs.merge(include_blank: true) %>
</span>
