<div class="contentPortalArticleCategory">

<div class="categories">
  <div class="h2">■カテゴリ一覧</div>
  <% @items.each do |item| %>
  <div>・<%= link_to h(item.title), "#{item.name}/" %></div>
  <% end %>
</div>

</div>
