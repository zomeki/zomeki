<div class="contentPortalGroupSiteBusiness">

<div class="wards">
  <div class="h2">■業種一覧</div>
  <% @items.each do |item| %>
  <div>・<%= link_to h(item.title), "#{item.name}/" %></div>
  <% end %>
</div>

</div>
