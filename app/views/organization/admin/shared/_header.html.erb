<div class="mainMenu">
  <%= render 'layouts/admin/cms/menu', :current => :content %>
</div>

<div class="contentMenu">
  <%= link_to "#{Organization::Group.model_name.human}一覧", organization_groups_path(content: @content), class: 'fixed' %>
  <%- if Core.user.has_auth?(:designer) -%>
    <%= link_to "#{Organization::Group.model_name.human}設定", organization_content_settings_path, class: 'fixed' %>
  <%- end -%>
</div>
