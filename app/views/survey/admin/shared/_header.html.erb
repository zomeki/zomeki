<div class="mainMenu">
  <%= render 'layouts/admin/cms/menu', :current => :content %>
</div>

<div class="contentMenu">
  <%= link_to "#{Survey::Form.model_name.human}一覧", survey_forms_path(content: @content), class: 'fixed' %>
</div>
